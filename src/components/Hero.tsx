import { motion } from "framer-motion";
import { styles } from "../styles";
import { ComputerCanvas } from "./canvas";

const Hero = () => {
  return (
    <div>
      <h2 className="text-red-200">Hero</h2>
    </div>
  );
};

export default Hero;
